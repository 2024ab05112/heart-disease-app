apiVersion: v1
kind: Service
metadata:
  name: prometheus
  labels:
    app.kubernetes.io/name: prometheus
    app.kubernetes.io/component: monitoring
    app.kubernetes.io/part-of: heart-disease-prediction
  annotations:
    service.beta.kubernetes.io/azure-dns-label-name: heart-disease-prom-2024ab05112
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: prometheus
    app.kubernetes.io/component: monitoring
  ports:
    - name: http
      port: 80
      targetPort: 9090
      nodePort: 30090
      protocol: TCP
